name: Backend CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest 

    steps:
      - name: Checkout code
        uses: actions/checkout@v3


      - name: Rebuild Docker
        run: docker compose -f ~/Desktop/Lx/apptraining-2/docker-compose.yml up --build -d
